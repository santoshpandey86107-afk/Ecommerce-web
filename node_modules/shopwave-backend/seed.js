import dotenv from 'dotenv';

dotenv.config();

console.log('Seed script is ready. The current app uses in-memory demo data, so no database seeding is required.');
console.log('You can start the app with: npm run dev');
